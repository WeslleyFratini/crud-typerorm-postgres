postgres:
  image: postgres:13
  ports:
    - 5432:5432
  environment:
    POSTGRES_USER: admin
    POSTGRES_PASSWORD: admin
    POSTGRES_DB: code_drops_crud
  volumes:
    -  data:/var/lib/postgresql/data    